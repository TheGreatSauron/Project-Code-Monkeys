#include "Spline.h"

Spline::Spline() : curve(0) {}

void Spline::offset(sf::Vector2f offset)
{
    std::queue<sf::Vector2f> tempQueue;

    for (sf::Vector2f currentVector : positionQueue)
    {

    }
}
